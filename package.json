{
  "name": "fookie",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "publish:changed": "node scripts/publish-changed.mjs",
    "site": "npx --yes serve site -p 4173"
  },
  "devDependencies": {
    "turbo": "^2.10.10"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.9.7",
  "overrides": {
    "typescript": "6.0.3"
  },
  "license": "MIT"
}
